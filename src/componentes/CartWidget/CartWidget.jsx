import cart from './img/cart.png';

const CartWidget = () => {
    return (
        <div>
            <img src={cart} alt="cartwidget" />
            0
        </div>
    )
}

export default CartWidget;
